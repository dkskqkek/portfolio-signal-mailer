# Signal Mailer Configuration
scheduler:
  # 매일 아침 몇시에 신호 확인할지 (시:분, 24시간 형식)
  run_time: "09:00"  # 한국시간 오전 9시 (미국 시간 기준 자동 변환)
  timezone: "Asia/Seoul"

email:
  # SMTP 설정 - Gmail 기준
  # Gmail의 경우 "앱 비밀번호" 생성 필요: https://myaccount.google.com/apppasswords
  smtp_server: "smtp.gmail.com"
  smtp_port: 587
  sender_email: "gamjatangjo@gmail.com"
  sender_password: "skekcgozubcmjjqq"
  
  # 수신자 설정
  recipient_email: "gamjatangjo@gmail.com"
  
  # 이메일 제목/본문 템플릿
  subject_template: "[포트폴리오 신호] {status}"
  
# Portfolio Strategy Configuration
# Current Strategy: QLD (2x Nasdaq) + Top-3 Defensive Ensemble
# Signal Logic: Dual SMA (110/250) with Hysteresis
# 
# Portfolio Weights (Hardcoded in signal_detector.py):
# - Tactical Asset: 45% (QLD in NORMAL, Top-3 Ensemble in DANGER)
# - KOSPI: 20%
# - SPY: 20%
# - GOLD: 15%
#
# Note: This section is for documentation only. 
# Actual weights are managed in signal_detector.py code.

# 신호 이력 저장 경로
history_file: "d:/gg/signal_mailer/signal_history.json"

# 로그 저장 경로
log_file: "d:/gg/signal_mailer/mailer.log"

# 디버그 모드 (True면 실제 메일 발송 안함)
debug_mode: false
